name: Deploy MultiDocker
on:
  push:
    branches:
      - main # check your repo, your default branch might be master!

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
      - run: docker build -t myk3s/react-test -f ./client/Dockerfile.dev ./client
      - run: docker run -e CI=true myk3s/react-test npm test

      - run: docker build -t myk3s/multi-client ./client
      - run: docker build -t myk3s/multi-nginx ./nginx
      - run: docker build -t myk3s/multi-server ./server
      - run: docker build -t myk3s/multi-worker ./worker

      - run: docker push myk3s/multi-client
      - run: docker push myk3s/multi-nginx
      - run: docker push myk3s/multi-server
      - run: docker push myk3s/multi-worker
